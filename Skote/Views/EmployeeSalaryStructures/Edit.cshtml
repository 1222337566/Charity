@model InfrastructureManagmentWebFramework.Models.Payroll.EmployeeStructures.EditEmployeeSalaryStructureVm
@using InfrastructureManagmentWebFramework.Models.HR.EmployeeProfile
@{ var header = ViewBag.EmployeeHeader as EmployeeHeaderVm; ViewData["Title"] = "تعديل بند المرتب"; }
@if (header != null)
{
    <partial name="~/Views/Shared/HR/_EmployeeHeader.cshtml" model="header" />
}
<h4 class="mb-3">تعديل بند المرتب</h4>
<form asp-action="Edit" method="post">
    <input asp-for="Id" type="hidden" />
    <div asp-validation-summary="All" class="text-danger mb-3"></div>
    <partial name="_Form" model="Model" />
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">حفظ</button>
        <a asp-action="Index" asp-route-employeeId="@Model.EmployeeId" class="btn btn-light">رجوع</a>
    </div>
</form>

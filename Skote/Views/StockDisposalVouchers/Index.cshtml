@model IEnumerable<InfrastrfuctureManagmentCore.Domains.Charity.StockAdvanced.StockDisposalVoucher>
<div class="d-flex justify-content-between align-items-center mb-3"><h4>الهالك والإعدام</h4><a asp-action="Create" class="btn btn-danger">مستند جديد</a></div>
<table class="table table-bordered"><thead><tr><th>الرقم</th><th>التاريخ</th><th>النوع</th><th>عدد الأصناف</th></tr></thead><tbody>
@foreach (var x in Model) { <tr><td>@x.VoucherNumber</td><td>@x.VoucherDate.ToString("yyyy-MM-dd")</td><td>@x.DisposalType</td><td>@x.Lines.Count</td></tr> }
</tbody></table>
